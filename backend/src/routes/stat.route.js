import { Router } from "express";

const router = Router();

router.get("/stats", (req, res) => {
  // Logic to get all stats
});

router.post("/stats", (req, res) => {
  // Logic to create a new stat
});

export default router;
